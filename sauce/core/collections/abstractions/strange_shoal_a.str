false &&
{	strange :
	{	<shoal> :: abstraction
		(	// dimensions
			#key :<<>># <>,
			#value :<<>># <>,
			(	// parent
				$$<collection>[ #key , #value, <flock> ],
				{	// shoal
					// mutable range
					mutate_begin_ := (<forward_mutator[<flock>]>),
					mutate_end_ := (<forward_mutator[<flock>]>),
				}
			)
		)
	}
}