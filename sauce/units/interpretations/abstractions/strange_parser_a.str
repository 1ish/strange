false &&
{	strange :
	{	<parser> :: abstraction
		(	// no dimensions
			(	// parent
				$$<>[],
				{	// parser
					parse_ := (tokenizer :<range[<>]>#, <expression>), //TODO <range[<token>]>
				}
			)
		)
	}
}