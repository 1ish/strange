false &&
{	strange :
	{	<any> :: abstraction
		(	// no dimensions
			(	// no parents
				{	
					// exposure
					extract_thing : "one_t const& extract_thing() const",
					mutate_thing : "one_t& mutate_thing()",

					// reflection
					type_ :# (<symbol>),
				}
			)
		)
	}
}